name = "vibecodr-openai-gateway"
main = "../../src/worker.ts"
compatibility_date = "2026-04-23"
compatibility_flags = ["nodejs_compat"]
workers_dev = false
preview_urls = false
routes = [
  { pattern = "openai.vibecodr.space/*", zone_name = "vibecodr.space" },
]

[observability]
enabled = true

[observability.logs]
head_sampling_rate = 1

[observability.traces]
enabled = true
head_sampling_rate = 0.05

[vars]
APP_BASE_URL = "https://openai.vibecodr.space"
VIBECDR_API_BASE = "https://api.vibecodr.space"
OAUTH_PROVIDER_NAME = "clerk"
OAUTH_CLIENT_ID = "replace_me"
OAUTH_ISSUER_URL = "https://vibecodr.space/__clerk/"
OAUTH_DISCOVERY_URL = "https://vibecodr.space/__clerk/.well-known/openid-configuration"
OAUTH_SCOPES = "openid profile email offline_access"
COOKIE_SECURE = "true"
ALLOW_MANUAL_TOKEN_LINK = "false"
NODE_ENV = "production"
ENABLE_CHATGPT_IMPORT_PATH = "true"
ENABLE_CODEX_IMPORT_PATH = "true"
ENABLE_PUBLISH_FROM_CHATGPT = "true"
MAX_REQUEST_BODY_BYTES = "8500000"
RATE_LIMIT_WINDOW_SECONDS = "60"
RATE_LIMIT_REQUESTS_PER_WINDOW = "240"
RATE_LIMIT_MCP_REQUESTS_PER_WINDOW = "120"
CODEMODE_ENABLED = "false"
CODEMODE_DEFAULT = "false"
CODEMODE_REQUIRE_DYNAMIC_WORKER = "true"
CODEMODE_ALLOW_NATIVE_FALLBACK = "false"
CODEMODE_MAX_EXECUTION_MS = "5000"
CODEMODE_MAX_OUTPUT_BYTES = "32768"
CODEMODE_MAX_LOG_BYTES = "8192"
CODEMODE_MAX_NESTED_CALLS = "5"
# Optional preregistered MCP client for an official public CLI.
# Use https URLs or loopback http URLs; loopback redirect matching ignores only the port.
# MCP_STATIC_CLIENT_ID = "replace_me"
# MCP_STATIC_CLIENT_SECRET = "replace_me_if_confidential"
# MCP_STATIC_CLIENT_REDIRECT_URIS = "http://127.0.0.1/oauth/callback/vibecodr,http://localhost/oauth/callback/vibecodr"

[[kv_namespaces]]
binding = "OPERATIONS_KV"
id = "replace_with_kv_namespace_id"
preview_id = "replace_with_kv_preview_namespace_id"

[[durable_objects.bindings]]
name = "AUTH_CODE_COORDINATOR"
class_name = "AuthorizationCodeCoordinator"

[[services]]
binding = "VIBE_API"
service = "vibecodr-api"
environment = "production"

# Code Mode Dynamic Worker Loader.
# Keep CODEMODE_ENABLED disabled until this binding is live in the target account.
# When enabled, this loader is only for MCP Code Mode search/execute.
# [[worker_loaders]]
# binding = "CODEMODE_WORKER_LOADER"

[[ratelimits]]
name = "GLOBAL_RATE_LIMITER"
namespace_id = "replace_global_ratelimit_namespace_id"
simple = { limit = 240, period = 60 }

[[ratelimits]]
name = "MCP_RATE_LIMITER"
namespace_id = "replace_mcp_ratelimit_namespace_id"
simple = { limit = 120, period = 60 }

[[migrations]]
tag = "v1"
new_sqlite_classes = ["AuthorizationCodeCoordinator"]
